# Render Blueprint configuration
services:
  - type: web
    name: vector-deploy-create-frontend-backend-1
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
